# 🔧 Установка Xcode Command Line Tools

## Проблема:
Git не работает, потому что не установлены Xcode Command Line Tools.

## Решение:

### Способ 1: Автоматическая установка (рекомендуется)

1. Откройте терминал
2. Выполните команду:
   ```bash
   xcode-select --install
   ```
3. Появится окно установки - нажмите "Установить"
4. Дождитесь завершения установки (5-15 минут)
5. После установки перезапустите терминал

### Способ 2: Через Xcode (если установлен)

1. Откройте Xcode
2. Перейдите в **Preferences** → **Locations**
3. Выберите **Command Line Tools** из выпадающего списка
4. Сохраните

### Способ 3: Проверка установки

После установки проверьте:

```bash
xcode-select -p
```

Должен показать путь типа: `/Library/Developer/CommandLineTools`

### Проверка git

После установки проверьте git:

```bash
git --version
```

Должно показать версию git, например: `git version 2.x.x`

---

## После установки:

Выполните команды для отправки изменений:

```bash
cd "/Users/vsevolodaleksandrovicgurenko/Documents/артема родионова /stroyka"
git add .
git commit -m "Обновление: добавлены скролл-анимации и фоновые элементы"
git push
```

---

## Альтернатива: Использовать GitHub Desktop

Если не хотите устанавливать Command Line Tools, можно использовать:

1. **GitHub Desktop** - графический интерфейс для git
   - Скачайте: https://desktop.github.com/
   - Установите и войдите в аккаунт GitHub
   - Откройте папку проекта
   - Нажмите "Commit" и "Push"

2. **VS Code с Git расширением**
   - Установите VS Code
   - Установите расширение Git
   - Используйте встроенный интерфейс git

