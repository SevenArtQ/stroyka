# 📤 Команды для отправки изменений на GitHub

## Если git репозиторий еще не инициализирован:

```bash
cd "/Users/vsevolodaleksandrovicgurenko/Documents/артема родионова /stroyka"

# Инициализация git
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit: проект готов к деплою"

# Переименование ветки в main
git branch -M main

# Подключение к удаленному репозиторию
git remote add origin https://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@github.com/SevenArtQ/stroyka.git

# Отправка на GitHub
git push -u origin main
```

---

## Если репозиторий уже инициализирован:

```bash
cd "/Users/vsevolodaleksandrovicgurenko/Documents/артема родионова /stroyka"

# Проверка статуса (какие файлы изменены)
git status

# Добавление всех измененных файлов
git add .

# Или добавление конкретных файлов:
# git add src/pages/Home.jsx
# git add src/pages/Home.css

# Создание коммита с описанием изменений
git commit -m "Обновление: добавлены скролл-анимации и фоновые элементы"

# Отправка изменений на GitHub
git push
```

---

## Если нужно принудительно отправить (если были конфликты):

```bash
git push -u origin main --force
```

⚠️ **Внимание:** Используйте `--force` только если уверены, что хотите перезаписать удаленную ветку!

---

## Проверка что все отправлено:

```bash
# Проверить статус
git status

# Проверить последние коммиты
git log --oneline -5

# Проверить подключение к удаленному репозиторию
git remote -v
```

---

## Быстрая команда (все в одной строке):

```bash
cd "/Users/vsevolodaleksandrovicgurenko/Documents/артема родионова /stroyka" && git add . && git commit -m "Обновление проекта" && git push
```

---

## Если git не видит изменения:

1. **Проверьте, что файлы не в .gitignore:**
   ```bash
   cat .gitignore
   ```

2. **Добавьте файлы принудительно:**
   ```bash
   git add -f имя_файла
   ```

3. **Проверьте статус:**
   ```bash
   git status
   ```

4. **Если файлы все еще не видны, проверьте что они сохранены:**
   - Убедитесь, что файлы сохранены в редакторе
   - Проверьте что файлы существуют: `ls -la src/`

---

## Настройка git (если еще не настроено):

```bash
# Установка имени пользователя
git config --global user.name "Ваше Имя"

# Установка email
git config --global user.email "ваш@email.com"
```

---

## Решение проблем:

### Ошибка: "fatal: not a git repository"
```bash
git init
```

### Ошибка: "remote origin already exists"
```bash
# Удалить старый remote
git remote remove origin

# Добавить новый
git remote add origin https://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@github.com/SevenArtQ/stroyka.git
```

### Ошибка: "xcode-select: note: No developer tools"
```bash
# Установить Xcode Command Line Tools
xcode-select --install
```

