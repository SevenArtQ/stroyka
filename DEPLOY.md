# 🚀 Инструкция по деплою на GitHub Pages

## Быстрый старт

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com) и создайте новый репозиторий
2. Назовите его, например, `stroyka` (или любое другое имя)

### Шаг 2: Настройте base path

Откройте файл `vite.config.js` и замените `/stroyka/` на название вашего репозитория:

```javascript
base: process.env.NODE_ENV === 'production' ? '/ваше-название-репозитория/' : '/',
```

**Важно:** Если репозиторий называется `stroyka`, то оставьте `/stroyka/` как есть.

### Шаг 3: Инициализируйте Git и загрузите код

```bash
# Инициализация Git
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Сайт строительной компании"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Добавьте удаленный репозиторий (замените на ваш URL)
git remote add origin https://github.com/ваш-username/название-репозитория.git

# Загрузите код
git push -u origin main
```

### Шаг 4: Включите GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** (Настройки)
3. В левом меню найдите **Pages**
4. В разделе **Source** выберите **GitHub Actions**
5. Сохраните изменения

### Шаг 5: Дождитесь деплоя

1. GitHub Actions автоматически запустится после первого push
2. Перейдите во вкладку **Actions** в вашем репозитории
3. Дождитесь завершения workflow (обычно 2-3 минуты)
4. После успешного деплоя сайт будет доступен по адресу:
   ```
   https://ваш-username.github.io/название-репозитория/
   ```

## 🔄 Обновление сайта

После каждого изменения просто делайте:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически задеплоит обновления!

## 🌐 Альтернативные варианты деплоя

### Vercel (рекомендуется для продакшена)

1. Зарегистрируйтесь на [Vercel](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Vercel автоматически определит настройки и задеплоит сайт
4. Получите бесплатный домен вида: `ваш-проект.vercel.app`

### Netlify

1. Зарегистрируйтесь на [Netlify](https://netlify.com)
2. Подключите GitHub репозиторий
3. Настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
4. Получите бесплатный домен: `ваш-проект.netlify.app`

## ❓ Решение проблем

### Сайт не открывается / 404 ошибка

1. Проверьте, что в `vite.config.js` указан правильный `base` path
2. Убедитесь, что GitHub Pages включен и использует GitHub Actions
3. Проверьте логи в разделе Actions

### Стили не загружаются

1. Убедитесь, что `base` path в `vite.config.js` совпадает с названием репозитория
2. Проверьте, что все пути к ресурсам относительные

### Роутинг не работает

1. Убедитесь, что файл `public/404.html` присутствует
2. Проверьте, что используется правильный `base` path

## 📞 Поддержка

Если возникли проблемы, проверьте:
- [GitHub Pages документация](https://docs.github.com/en/pages)
- [Vite документация](https://vitejs.dev/guide/static-deploy.html#github-pages)

