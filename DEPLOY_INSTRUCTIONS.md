# 🚀 Быстрая инструкция по деплою

## Шаг 1: Установите Xcode Command Line Tools

Откройте терминал и выполните:

```bash
xcode-select --install
```

Следуйте инструкциям на экране. Это займет несколько минут.

## Шаг 2: Запустите скрипт деплоя

После установки Xcode Command Line Tools выполните:

```bash
cd "/Users/vsevolodaleksandrovicgurenko/Documents/артема родионова /stroyka"
./deploy.sh
```

Или вручную выполните команды:

```bash
# Инициализация git
git init
git add .
git commit -m "Deploy to GitHub Pages"

# Настройка ветки
git branch -M main

# Подключение к GitHub (токен уже включен)
git remote add origin https://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@github.com/SevenArtQ/stroyka.git

# Отправка на GitHub
git push -u origin main
```

## Шаг 3: Включите GitHub Pages

1. Перейдите на: https://github.com/SevenArtQ/stroyka/settings/pages
2. В разделе **Source** выберите **GitHub Actions**
3. Нажмите **Save**

## Шаг 4: Дождитесь деплоя

1. Перейдите во вкладку **Actions** в репозитории
2. Дождитесь завершения workflow "Deploy to GitHub Pages" (2-3 минуты)
3. После успешного деплоя сайт будет доступен по адресу:

   **https://sevenartq.github.io/stroyka/**

## ✅ Готово!

После этого при каждом push в ветку `main` сайт будет автоматически обновляться!

---

## 🔒 Безопасность

⚠️ **Важно:** Токен в скрипте `deploy.sh` - это временное решение. После первого деплоя рекомендуется:

1. Удалить токен из скрипта
2. Настроить SSH ключи для GitHub
3. Или использовать GitHub CLI для аутентификации

## 📝 Обновление сайта

Для обновления сайта просто выполните:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически задеплоит изменения!

