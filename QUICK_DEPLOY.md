# 🚀 Быстрый деплой за 2 минуты

## Вариант 1: Vercel (САМЫЙ БЫСТРЫЙ ⚡)

### Через веб-интерфейс (без установки):

1. **Перейдите на [vercel.com](https://vercel.com)**
2. **Войдите через GitHub** (или создайте аккаунт)
3. **Нажмите "Add New Project"**
4. **Импортируйте ваш репозиторий:**
   - Если репозиторий уже на GitHub - выберите его
   - Если нет - сначала загрузите на GitHub (см. ниже)
5. **Настройки проекта:**
   - Framework Preset: **Vite**
   - Root Directory: `./` (оставьте как есть)
   - Build Command: `npm run build` (уже указано)
   - Output Directory: `dist` (уже указано)
6. **Нажмите "Deploy"**
7. **Готово!** Через 30 секунд получите ссылку вида: `https://ваш-проект.vercel.app`

### Через Vercel CLI (если установлен):

```bash
npm i -g vercel
vercel
```

**Преимущества Vercel:**
- ✅ Деплой за 30 секунд
- ✅ Автоматический деплой при каждом push
- ✅ Бесплатный SSL сертификат
- ✅ Глобальный CDN
- ✅ Не нужно настраивать base path

---

## Вариант 2: Netlify (тоже очень быстро)

### Через веб-интерфейс:

1. **Перейдите на [netlify.com](https://netlify.com)**
2. **Войдите через GitHub**
3. **Нажмите "Add new site" → "Import an existing project"**
4. **Выберите ваш GitHub репозиторий**
5. **Настройки:**
   - Build command: `npm run build`
   - Publish directory: `dist`
6. **Нажмите "Deploy site"**
7. **Готово!** Ссылка: `https://случайное-имя.netlify.app`

### Или через Netlify Drop (drag & drop):

1. Запустите локально: `npm run build`
2. Перейдите на [app.netlify.com/drop](https://app.netlify.com/drop)
3. Перетащите папку `dist` в браузер
4. Готово!

---

## Вариант 3: Cloudflare Pages

1. **Перейдите на [pages.cloudflare.com](https://pages.cloudflare.com)**
2. **Войдите через GitHub**
3. **Создайте новый проект**
4. **Выберите репозиторий**
5. **Настройки:**
   - Framework preset: **Vite**
   - Build command: `npm run build`
   - Build output directory: `dist`
6. **Deploy**

---

## Вариант 4: GitHub Pages (уже настроен)

Если репозиторий уже на GitHub:

1. **Settings → Pages**
2. **Source: GitHub Actions**
3. **Дождитесь деплоя** (2-3 минуты)
4. **Ссылка:** `https://ваш-username.github.io/stroyka/`

---

## 🎯 Рекомендация

**Для быстрого показа демо:** используйте **Vercel** - самый быстрый способ (30 секунд)

**Для постоянного хостинга:** GitHub Pages (уже настроен) или Vercel

---

## 📝 Если репозитория еще нет на GitHub:

```bash
# 1. Создайте репозиторий на GitHub (через веб-интерфейс)

# 2. Затем выполните:
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/название-репозитория.git
git push -u origin main
```

---

## ⚡ Самый быстрый способ (1 минута):

1. Запустите: `npm run build`
2. Перейдите на [app.netlify.com/drop](https://app.netlify.com/drop)
3. Перетащите папку `dist` в браузер
4. Готово! Ссылка появится сразу

**Это самый быстрый способ показать сайт без регистрации!**

