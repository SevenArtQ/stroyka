# 🚀 Инструкция по деплою на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages!

## ✅ Что уже настроено:

1. ✅ `vite.config.js` - base path установлен на `/stroyka/`
2. ✅ `.github/workflows/deploy.yml` - GitHub Actions workflow создан
3. ✅ `.gitignore` - настроен для исключения ненужных файлов

## 📋 Шаги для деплоя:

### 1. Установите Git (если еще не установлен)

На macOS:
```bash
xcode-select --install
```

Или скачайте с официального сайта: https://git-scm.com/downloads

### 2. Инициализируйте Git репозиторий

```bash
cd "/Users/vsevolodaleksandrovicgurenko/Documents/артема родионова /stroyka"
git init
git add .
git commit -m "Initial commit: готов к деплою на GitHub Pages"
```

### 3. Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Название: `stroyka` (или другое, но тогда нужно изменить base path)
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите "Create repository"

### 4. Подключите локальный репозиторий к GitHub

```bash
git branch -M main
git remote add origin https://github.com/ваш-username/stroyka.git
git push -u origin main
```

**Замените `ваш-username` на ваш GitHub username!**

### 5. Включите GitHub Pages

1. Перейдите в Settings вашего репозитория на GitHub
2. В левом меню выберите **Pages**
3. В разделе **Source** выберите **GitHub Actions**
4. Сохраните изменения

### 6. Дождитесь деплоя

- GitHub Actions автоматически запустится при первом push
- Перейдите во вкладку **Actions** в репозитории
- Дождитесь завершения workflow (обычно 2-3 минуты)
- После успешного деплоя сайт будет доступен по адресу:
  ```
  https://ваш-username.github.io/stroyka/
  ```

## 🔄 Автоматический деплой

После настройки, при каждом push в ветку `main` сайт будет автоматически обновляться!

```bash
git add .
git commit -m "Описание изменений"
git push
```

## ⚙️ Если репозиторий называется не `stroyka`

Если ваш репозиторий называется по-другому, измените `base` path в `vite.config.js`:

```javascript
base: '/название-вашего-репозитория/',
```

## 🐛 Решение проблем

### GitHub Actions не запускается
- Убедитесь, что вы включили GitHub Pages в настройках репозитория
- Проверьте, что выбрали "GitHub Actions" как источник

### Сайт не открывается
- Проверьте правильность base path в `vite.config.js`
- Убедитесь, что workflow завершился успешно
- Подождите несколько минут после деплоя (может быть задержка)

### Ошибки при сборке
- Убедитесь, что все зависимости установлены: `npm install`
- Проверьте логи в разделе Actions на GitHub

## 📝 Примечания

- Для локальной разработки можно временно изменить `base: '/'` в `vite.config.js`
- После изменений не забудьте вернуть `base: '/stroyka/'` перед коммитом
- Или используйте отдельную ветку для разработки

## ✨ Готово!

После выполнения всех шагов ваш сайт будет доступен на GitHub Pages!

