# 🚀 Инструкция по деплою для репозитория stroyka

Ваш репозиторий: https://github.com/SevenArtQ/stroyka

## Шаг 1: Загрузите код на GitHub

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация Git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Сайт строительной компании"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Добавьте удаленный репозиторий
git remote add origin https://github.com/SevenArtQ/stroyka.git

# Загрузите код
git push -u origin main
```

## Шаг 2: Выберите способ деплоя

### Вариант А: Vercel (РЕКОМЕНДУЕТСЯ - самый быстрый ⚡)

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "Add New Project"
4. Выберите репозиторий `SevenArtQ/stroyka`
5. Настройки (уже настроены автоматически):
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. **ВАЖНО:** В настройках проекта добавьте переменную окружения:
   - Name: `VITE_DEPLOY_TARGET`
   - Value: `vercel`
7. Нажмите "Deploy"
8. **Готово!** Через 30 секунд получите ссылку: `https://stroyka-xxx.vercel.app`

**Или просто измените в `vite.config.js`:**
```javascript
base: '/',  // Для Vercel
```

### Вариант Б: GitHub Pages (бесплатный хостинг)

1. После загрузки кода на GitHub:
   - Перейдите в репозиторий: https://github.com/SevenArtQ/stroyka
   - Откройте **Settings** (Настройки)
   - В левом меню найдите **Pages**
   - В разделе **Source** выберите **GitHub Actions**
   - Сохраните

2. GitHub Actions автоматически запустится
3. Перейдите во вкладку **Actions** в репозитории
4. Дождитесь завершения workflow (2-3 минуты)
5. После успешного деплоя сайт будет доступен:
   ```
   https://sevenartq.github.io/stroyka/
   ```

### Вариант В: Netlify (тоже быстро)

1. Перейдите на [netlify.com](https://netlify.com)
2. Войдите через GitHub
3. "Add new site" → "Import an existing project"
4. Выберите репозиторий `SevenArtQ/stroyka`
5. Настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
6. **ВАЖНО:** В настройках проекта добавьте переменную окружения:
   - Name: `VITE_DEPLOY_TARGET`
   - Value: `netlify`
7. Deploy
8. **Готово!** Ссылка: `https://xxx.netlify.app`

**Или измените в `vite.config.js`:**
```javascript
base: '/',  // Для Netlify
```

## Шаг 3: Обновление сайта

После каждого изменения:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт автоматически обновится!

## ⚡ Самый быстрый способ (1 минута, без регистрации):

1. Запустите: `npm run build`
2. Перейдите на [app.netlify.com/drop](https://app.netlify.com/drop)
3. Перетащите папку `dist` в браузер
4. **Готово!** Ссылка появится сразу

---

## 📝 Примечания

- Base path в `vite.config.js` уже настроен для репозитория `stroyka`
- Для Vercel/Netlify можно изменить на `base: '/'`
- GitHub Pages будет работать с текущими настройками

