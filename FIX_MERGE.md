# 🔧 Исправление конфликта веток

## Выполните эти команды:

```bash
# 1. Настроить стратегию merge
git config pull.rebase false

# 2. Получить изменения с GitHub и объединить
git pull origin main --allow-unrelated-histories

# 3. Если будут конфликты, разрешите их, затем:
git add .
git commit -m "Merge remote and local changes"

# 4. Отправить на GitHub
git push -u origin main
```

## Или проще (если не важно что на GitHub):

```bash
git push -u origin main --force
```

Это перезапишет все на GitHub вашими локальными файлами.

